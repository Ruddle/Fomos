


[package]
name = "loader"
version = "0.1.0"
edition = "2021"

[build-dependencies]
bootloader = "0.11.3"
kernel = { path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }
# 
[dependencies]

[workspace]
members = ["kernel"]